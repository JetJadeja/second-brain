import type { BotContext } from '../../context.js'

/** Stub — replaced in Phase C.1 */
export async function handleSaveContent(ctx: BotContext): Promise<void> {
  await ctx.reply('Save content handler not yet implemented.')
}
