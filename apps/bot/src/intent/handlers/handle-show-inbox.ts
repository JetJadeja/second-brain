import type { BotContext } from '../../context.js'

/** Stub — replaced in Phase C.3 */
export async function handleShowInbox(ctx: BotContext): Promise<void> {
  await ctx.reply('Show inbox handler not yet implemented.')
}
